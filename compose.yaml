services:
  php:
    build:
      context: .
      target: php

  nginx:
    build:
      context: .
      target: nginx
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - php
